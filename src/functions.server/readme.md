Functions in here should be called:

- on the server: directly
- on the client: wrapped with useServerFn()

loader data in the Route component of each page runs on the server so should call the functions directly
